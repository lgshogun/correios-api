spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/study_db
    username: postgres
    password:
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
  springdoc:
    api-docs:
      path: /api-docs
    swagger-ui:
      path: /swagger-ui.html